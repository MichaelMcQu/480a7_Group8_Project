# 480a7_Group8_Project



## Background
The purpose of this experiemnt was to see the potential ways productivity might of shifted with developers during a pre-COVID time period compared to post-COVID time period.  


## Instructions for Study Replication

* Clone repository and be sure that youre using a up-to date version of Python
* Once scripts are loaded into desired IDE run the Python script <insert_name> to get the data into a .csv file
* From there run the second Python script named <insert_name> this is how we plotted the data using matplotlib (Note: the name of the file should match the strings being passed referencing the CSVs)
* The output from the Python script should be a plotted graph representing the corrolation between hours or time it took to merge a PR during a specific month within the year.
* These steps are repeated to find get the data for commit frequency